#!/bin/sh
# Deliberately present, never executed: the importer reads only the Markdown and
# lists every other entry as "not imported". Nothing in an archive is run.
echo "if you can read this in a review run, the import boundary is broken"
