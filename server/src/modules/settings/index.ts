/** Public surface of the settings module — other modules import from here, not from its internals. */
export { resolveFeatureModel, getFeatureModelOverride, defaultFeatureModel } from './feature-models.js';
